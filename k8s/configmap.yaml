apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-support-config
  namespace: ai-support
  labels:
    app: ai-support-api
data:
  APP_NAME: "AI Customer Support Agent Platform"
  APP_ENV: "production"
  APP_VERSION: "1.0.0"
  DEBUG: "false"
  ACCESS_TOKEN_EXPIRE_MINUTES: "60"

  # Database — host/port only; credentials come from Secret
  # Groq
  GROQ_MODEL: "llama-3.3-70b-versatile"
  GROQ_FALLBACK_MODEL: "llama-3.1-8b-instant"

  # Pinecone
  PINECONE_ENVIRONMENT: "us-east-1"
  PINECONE_INDEX_NAME: "support-kb-index"
  EMBEDDING_DIMENSION: "384"

  # Embeddings
  EMBEDDING_MODEL: "sentence-transformers/all-MiniLM-L6-v2"
  TRANSFORMERS_CACHE: "/app/.cache"

  # MLflow
  MLFLOW_TRACKING_URI: "http://mlflow-svc:5000"
  MLFLOW_EXPERIMENT_NAME: "ai-support-agent"

  # Retrieval
  MAX_RETRIEVAL_DOCS: "5"
  SIMILARITY_THRESHOLD: "0.65"
