apiVersion: v1
kind: Service
metadata:
  name: ai-support-api-nodeport
  namespace: ai-support
spec:
  type: NodePort
  selector:
    app: ai-support-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
